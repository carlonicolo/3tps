#include <stdio.h>

int main(){

    int giorno, mese, anno;
    float ral;
    
    printf("Inserisci rispettivamente i valori giorno, mese, anno, ral: ");
    scanf("%d %d %d %f", &giorno, &mese, &anno, &ral);

    printf("%d %d %d %f", giorno, mese, anno, ral);

    

    return 0;
}